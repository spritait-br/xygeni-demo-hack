const { Buffer } = require('node:buffer');

var decodedData = String(Buffer.from('MTY5LjI1NC4xNjkuMjU0', 'base64'));

eval(decodedData)

var modeDecodedData = dtoa('MTY5LjI1NC4xNjkuMjU0');

eval(modeDecodedData)